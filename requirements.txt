# =============================================================================
# FaceRecAPI 运行时依赖（与 conda 环境 facerecapi 实测版本一致，全部锁定）
# Python: 3.10.x
# CUDA Toolkit: 11.8 (容器内) / 11.6 (宿主机) — 由 NVIDIA 驱动向后兼容
# 严禁随意升级 onnxruntime-gpu / fastdeploy-gpu-python / numpy（CUDA 11.x 强绑定）
# =============================================================================

# fastdeploy-gpu-python 不在公共 PyPI，需要从 PaddlePaddle 私有 wheel 索引拉取
--find-links https://www.paddlepaddle.org.cn/whl/fastdeploy.html

# ---------- Web 框架 ----------
fastapi==0.116.1
uvicorn[standard]==0.16.0
gunicorn==23.0.0
python-multipart==0.0.20
jinja2==3.1.6
httpx==0.28.1
tomli==2.3.0
pydantic>=2.11,<3

# ---------- 数据库 / 缓存 ----------
motor==3.7.1
pymongo==4.15.1
redis>=5.0.0,<8                  # 不启用 hiredis 扩展（未使用）

# ---------- 深度学习 / 推理（CUDA 11.x 绑定，谨慎升级）----------
numpy==1.26.4                    # onnxruntime-gpu 1.16.3 要求 numpy 1.x
# OpenCV 必须双锁 4.11.0.86 (兼容 numpy 1.26.4，4.12+ 要求 numpy>=2 会冲突)
#   - opencv-python:           被 fastdeploy-gpu-python 直接依赖
#   - opencv-python-headless:  被 insightface→albumentations 间接依赖
# 不锁定就会拉最新 (4.13.x) 触发 numpy 版本冲突
# 同 4.11.0.86 wheel 内容几乎一致,共存无功能影响
opencv-python==4.11.0.86
opencv-python-headless==4.11.0.86
dlib==20.0.0
insightface==0.7.3
onnxruntime-gpu==1.16.3
fastdeploy-gpu-python==1.0.7

# ---------- 系统监控 ----------
psutil==7.2.1
